name: CI

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.kiru/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - main
      - dev
    paths:
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.kiru/**'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: rust:alpine
    steps:
      - name: Install Build Dependencies
        run: apk add --no-cache git

      - name: Install Rust Tools
        run: |
          rustup component add clippy rustfmt

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Run CI via Kiru
        run: cargo run -- run ci -c .kiru/main.kiru
