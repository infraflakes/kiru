name: CI

on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.kiru/**'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.kiru/**'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: rust:alpine
    steps:
      - name: Install Build Dependencies
        run: apk add --no-cache git

      - name: Install Rust Tools
        run: |
          rustup component add clippy rustfmt

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Compile Kiru Config
        run: cargo run -- compile -c main.kiru -o .

      - name: Run CI via Kiru
        run: cargo run -- run ci_kiru -p kirufile
