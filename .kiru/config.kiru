var shell workdir = `echo $HOME/Projects`;

var string kiru_dir = `${workdir}/kiru`;
pr kiru [
  url = `https://github.com/infraflakes/kiru.git`
  dir = $kiru_dir
  sync = `clone`
  branch = `dev`
] {}
import `${kiru_dir}/main.kiru`;
