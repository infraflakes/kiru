pr kiru {
  run all {
    kiru::test => kiru::build;
    kiru::fmt => kiru::test;
    kiru::fail => kiru::fmt;
  }
  run test {
    kiru::test;
    kiru::build;
  }
  run ci {
    kiru::test => kiru::build;
  }
}
